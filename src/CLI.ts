import { resolve, basename } from 'path';
import {
  readdirSync,
  existsSync,
  mkdirSync,
} from 'fs';
// import { Migration } from '../dist';         

export class CLI {
  folder: string;

}

export default CLI;
